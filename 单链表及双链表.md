#单链表:使用数组模拟. head记录头结点的位置.

```c++
int e[N],ne[N],head,idx;

void init() //初始化函数
{
    head = -1;
    idx = 0;
}
//x    1     2     3 head指向头结点,插入过程如下
//    head
void insert(int x)//在链表头插入一个元素
{
    e[idx] = x;
    ne[idx] = head;
    head = idx++;
}
//i k j 
void remove_list(int k)//删除链表中位置为k的元素
{
    if(k == head) head = ne[head];
    else ne[k - 1] = ne[k];
}


```

#双链表:使用数组模拟,使用L数组和R数组模拟节点之间的双向箭头 

```c++
int e[N],l[N],r[N],idx;
int init()
{
    l[1] = 0;
    r[0] = 1;
    idx = 2;//让头结点和尾节点分别为0  和  1;
}
//k x r[k] 右侧插入 分别将节点的线连接即可
int insert_head(int k,int x)
{
    e[idx] = x;
    l[idx] = k;
    r[idx] = r[k];
    l[r[k]] = idx;
    r[k] = idx;
    idx ++;
}
//a k b 将k节点删除
int remove_list(int k)
{
    r[l[k]] = r[k];
    l[r[k]] = l[k];
}
```

##十进制数转换成二进制数

```c++
void binary(int x)
{
    if(x > 1) binary(x / 2);
    printf("%d",x % 2);
}
```


