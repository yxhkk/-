#二分算法

##用途:通过不断缩小搜索范围来快速逼近答案

###原理:设置check函数,更新边界(l,r)范围.继续取中点逼近.

####整数二分

```c++
int bsearch_1(int l,int r)
{
    while(l < r)
    {
        int mid = l + r + 1 >> 1;
        if(check(mid)) l = mid;
        r = mid - 1;
    }
    return l;
}
```

```c++
int bsearch_1(int l,int r)
{
    while(l < r)
    {
        int mid = l + r >> 1;
        if(check(mid)) r = mid;
        l = mid + 1;
    }
    return l;
}
```

####浮点数二分

```c++
double bsearch_3(int l,int r)
{
    const double eps = 1e-6;
    while(r - l > eps)
    {
        double mid =( l + r ) / 2;
        if(check(mid)) l = mid;
        else r = mid;
    }
    return l;
}
```
