#高精度算术

##高精度加法

###原理:将大位数从个位开始一次输入进数组,使用数组模拟数字计算过程.

```c++
string a,b;
cin >> a >> b;
vector <int> A,B;
for(int i = a.size();i >= 0;i --) A.push_back(a[i]);
for(int i = b.size();i >= 0;i --) B.push_back(b[i]);

vector<int> add(vector<int> &A,vector<int> &B)
{
    vector<int> C;
    int t = 0;//表示进位,初始化为0
    if(B.size() > A.size()) return add(B,A);
    for(int i = 0;i < A.size();i ++)
    {
        t += A[i];
        if(i < B.size()) t += B[i];
        C.push_back(t % 10);
        t /= 10;
    }
    if(t) C.push_back(t);
    return C;
}
```

##高精度减法

###原理:同加法

```c++
string a,b;
cin >> a >> b;
vector <int> A,B;
for(int i = a.size();i >= 0;i --) A.push_back(a[i]);
for(int i = b.size();i >= 0;i --) B.push_back(b[i]);


vector<int> sub(vector<int> &A,vector<int> &B)
{
    int t = 0;
    vector<int> C;
    if(B.size() > A.size()) return sub(B,A); //结果不完整!!!
    for(int i = 0;i < A.size();i ++)
    {
        t += A[i];
        if(i < B.size()) t -= B.size();
        C.push_back( (t + 10) %10 );
        if(t < 0) t = -1
        else t = 0;
    }
    while (C.size() > 1 && C.back() == 0) C.pop_back();//去除前导0
    return C;
}
```

##高精度乘法:高精度乘低精度

###原理:大数每一位乘小数

```c++
string a;
int b;
cin >> a >> b;
vector <int> A;
for(int i = a.size();i >= 0;i --) A.push_back(a[i]);


vector<int> mul(vector<int> &A,int b)
{
    int t = 0;
    vector<int> C;
    for(int i =0;i < A.size() || t; i ++)
    {
        t += A[i] * b;
        C.push_back(t % 10);
        t /= 10;
    }
    while (C.size() > 1 && C.back() == 0) C.pop_back();
    return C;
}
```

##高精度除法:高精度除低精度

```c++
string a;
int b;
cin >> a >> b;
vector <int> A;
for(int i = a.size();i >= 0;i --) A.push_back(a[i]);


vector<int> div(vector<int< &A,int b)
{
    int t = 0;
    vector<int> C;
    for(int i = A.size() - 1;i >= 0;i --)
    {
        t = t * 10 + A[i];
        C.push_back(t / b);
        t %= b;
    }
    reserve(C.begin(),C.end());
    while (C.size() > 1 && C.back() == 0) C.pop_back();
    return C;
}
```
