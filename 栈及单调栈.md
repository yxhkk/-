#栈:使用数组模拟, [              tt]

```c++
int stk[N],tt = 0;
//入栈x
stk[++ tt] = x;
//出栈
tt --;
//栈顶元素
stk[tt];
//判断栈是否为空
if(tt > 0)
{
    //不为空
}
```

#单调栈:通过栈对原参差不齐的数据进行优化至一个满足题目条件的具有单调性的栈.

##常见模型:找出每个数左边第一个比它大(小)的数

```c++
int stk[N],tt = 0;

for(int i = 1;i <= n;i ++)
{
    while(tt && check(stk[tt]) tt --;
    stk[++ tt] = i;
}
```
