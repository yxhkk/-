#并查集:将一个集合的元素都写入一个数中,合并过程中对各个节点的路径进行优化,即将一个集合中的所有字节点都指向其共同的祖宗节点.

##用途:集合合并 以及 查询

```c++
int p[N];//存储节点对应的祖宗节点
//初始化函数
void init()//从1开始
{
    for(int i = 1;i <= n;i ++)
    {
        p[i] = i;
    }
}

//查找祖宗
int find(int x)
{
    if(p[x] != x) p[x] = find(p[x]);
    return p[x];
}

//合并操作a,b
p[find(a)] = find(b);

//查询操作
bool query(int a,int b)
{
    if(find(a) == find(b)) return true;
    else return false;
}


//维护结合大小的并查集 size只对祖宗节点有用
int p[N],size[N];

//初始化
for(int i = 1;i <= n;i ++)
{
    p[i] = i;
    size[i] = 1;
}

//合并操作a,b;
p[find(a)] = find(b);
size[find(b)] += size[find(a)];

//维护节点到祖宗距离的并查集
int p[N],d[N];

//初始化
for(int i = 1;i <= n; i++)
{
    p[i] = i;
    d[i] = 0;
}

//查找祖宗:
int find(int x)
{
    if(x != p[x])
    {
        int u = find(p[x]);
        d[x] += d[p[x]];
        p[x] = u;
    }
    return p[x];
}

//合并区间
p[find(a)] = find(b);
d[find(a)] = distance(find(b));
```


