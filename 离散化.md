#离散化:存储稀疏数组的一种方法.将离散的数组映射到连续的数组之中(1,2,3 ^^^ n)

##原理:对需要离散化的数组all进行排序以及去重,然后使用二分查找每个原理离散化之后对应的位置并返回.

```c++
vector<int> alls;
sort(alls.begin(),sort.end());
alls.erase(unique(alls.begin(),alls.end()),alls.end());//排序并去重


int find(int x)//寻找第一个大于等于x的位置,并将x离散化成l+1
{
    int l = 0,r = alls.size() - 1;
    while(l < r)
    {
        int mid = l + r << 1;
        if(alls[mid] >= x) r = mid;
        else l = mid + 1;
    }
    return l + 1;
}
```


