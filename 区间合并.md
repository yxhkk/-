#区间合并

##原理:区间合并一共三种情况,维护区间两侧范围,将合并的新区间送入原来区间组中即可

```c++
typedef pair<int,int> PII;
vector<PII> segs;
void merge_seg(vector<PII> &segs)
{
    vector<PII> res;
    sort(segs.begin(),segs.end());//已经一first为基准进行排序,所以不必担心左半边区间的各种问题只需要考虑右边区间的范围即可
    int st = -2e9,ed = -2e9
    for(auto seg : segs)
    {
        if(ed < seg.first)
        {
            if(st != -2e9) res.push_back({st,ed});
            st = seg.first,ed = seg.second;
        }
        else
        {
            ed = max(ed,seg.second);
        }
    }
    if(st != -2e9) res.push_back({st,ed});
}
```
