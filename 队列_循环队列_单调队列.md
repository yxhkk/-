#队列:使用数组模拟

##模型:tt  1 2 3   hh  插入顺序:  <-

```c++
int q[N],hh = -1,tt = 0;

//向队尾插入一个x
q[++ tt] = x;
//从队头弹出一个数
hh ++;
//队头的值
q[hh];
//判断队列是否为空
if(hh <= tt)
{
    //不为空
}
```

#循环队列:使用数组模拟

##模型: tt 1 2  3 hh

```c++
int q[N],hh = 0,tt = 0;

//向队尾插入一个x
q[tt ++] = x;
if(tt == N) tt = 0;
//从对头弹出一个元素
hh ++;
if(hh == N) hh = 0;
//对头的值
q[hh];
//判断队列是否为空
if(hh != tt)
{
    //不为空
}
```

#单调队列:同单调栈

##常见模型:滑动窗口中寻找最大最小值

```c++
int q[N],hh = 0,tt = 0;
for(int i = 0;i < n;i ++)
{
    while(hh <= tt && check(q[tt])) tt --;
    while(hh <= tt && check(q[hh])) hh ++;
    q[ ++tt] = i;
}
```


