# 快速排序

## 原理:选取一个数字作为standard,设置双指针,向standard收缩.不能收缩且i < j就交换数值.递归进行

```c++
void quick_sort(int a[],int l,int r)
{
    if(l >= r) return ;
    int i = l - 1,j = r + 1;
    int x = a[l + r  >> 1];
    while(i < j)
    {
        do i ++ while(a[i] < x)
        do j -- while(b[i] > x)
        if(i < j) swap(a[i],a[j]);
    }
    quick_sort(a,l,j);
    quick_sort(a,j + 1,r);
}
```

#归并排序

##原理:取mid,将左右两个数组进行排序形成新数组,递归进行.

```c++
int a[];
int tmp[],k;

void merge_sort(int a[],int l,int r)
{
    if(l >= r) return ;
    int mid = l + r >> 1;
    merge_sort(a,l,mid);
    merge_sort(a,mid + 1,r);
    int i = l,j = mid  +1;
    while(i <= mid && j <= r)
    {
        if(a[i] > a[j]) tmp[k ++] = a[i ++];
        else tmp[k ++] = a[j ++];
    }
    while(i <= mid) tmp[k ++] = a[i ++];
    while(j <= r) tmp[k ++] = a[j ++];
    for(int i = l,j = 0;i <= r;i ++,j ++) a[i] = tmp[j];
}
```


