#堆

##原理:使用h[]存储堆的元素,ph[k]和hp[k]分别映射第k个插入的元素在堆中的下标,堆中下标为k的元素是第几个插入的.

```c++
// h[N]存储堆中的值, h[1]是堆顶，x的左儿子是2x, 右儿子是2x + 1
// ph[k]存储第k个插入的点在堆中的位置
// hp[k]存储堆中下标是k的点是第几个插入的int h[N],ph[N],hp[N],size;
// idx表示插入的个数
void heap_swap(int a,int b)//a,b均为下标
{
    swap(ph[hp[a]],ph[hp[b]]);
    swap(hp[a],hp[b]);
    swap(h[a],h[b]);
}    
//down操作,以小根堆为例
void down(int k)
{
    int u = k;
    if(k * 2 <= size && h[k*2] < h[u]) u = 2 * k;
    if(k * 2 + 1 <= size && h[k * 2 + 1] < h[u]) u = 2 * k + 1;
    if(u != k)
    {
        heap_swap(u,k);
        down(u);
    }
}


void up(int k)
{
    while(k / 2 && h[k] < h[k/2])
    {
        heap_swap(k,k/2);
        k >>= 1;
    }
}

//向堆中插入元素
void insert(int x)
{
    idx ++;
    size ++;
    h[size] = x;
    ph[idx] = size;
    hp[size] = idx;
    up(size);
}
//删除第k个插入的元素
void remove(int k)
{
    int t = ph[k];
    heap_swap(t,size);
    sz --;
    down(t);
    up(t);
}

//修改第k个插入的元素为k
void change(int k,int x)
{
    int t = ph[k];
    h[t] = x;
    donw(t);
    up(t); 
}


```


