#一维前缀和

##原理:a数组为原始数组,数组遵循公式s[i] = s[i-1] + a[i],这样可以快速计算(l,r)区间内的数字之和

前缀和同样在字符串哈希算法中也有使用

###公式:

S[i] = a[1] + a[2] + ... a[i]
a[l] + ... + a[r] = S[r] - S[l - 1]



#二维前缀和

##原理:s[i,j]表示坐标为(i,j)左上角的所有元素之和 a[][]数组为原始数组.可快速计算(x1,y1) (x2,y2) 之间的元素之和

###公式:

s[i,j] = s[i - 1][j] + s[i][j - 1] - s[i-1][j-1] + a[i][j]

s[x2][y2] - s[x1-1][y2] - s[x2][y1-1] + s[x1-1][y1-1]



#一维差分

##原理:现有前缀和数组a 以及 原数组b.  a的前缀和对应原数组b. 如果我想对原数组(l,r)都加上c. 那么只需要对a[l]+=c 然后a[r+1] -= c. 这样即满足某个区间都+c又保持了计算前缀和后b数组的其余数值不变

###公式:

给区间[l, r]中的每个数加上c：A[l] += c, A[r + 1] -= c 



#二位差分:与一维差分的数据结构相同,有前缀和数组A,以及原数组B. 二位差分所做的操作为将(x1,y1) __ (x2,y2)之间的所有元素都+c

###公式:

给以(x1, y1)为左上角，(x2, y2)为右下角的子矩阵中的所有元素加上c：
S[x1, y1] += c, S[x2 + 1, y1] -= c, S[x1, y2 + 1] -= c, S[x2 + 1, y2 + 1] += c




