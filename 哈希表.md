#哈希表

```c++
//一般使用离N最近的质数计算哈希值
bool isPrime(int x)
{
    if(x < 2) return false;
    for(int i = 2;i * i <= x;i ++)
    {
        if(x % i == 0) return false;
    }
    return true;
}

int hash_prime(int x)
{
    if(x <= 2) return 2;
    while(!isPrime(x)) x++;
    return x;
}

//拉链法:h[]记录每个链表的头结点(相当于单链表的head)
int h[N],e[N],ne[N],idx;

void insert(int x)
{
    int k = (x % N + N) % N;
    e[idx] = x;
    ne[idx] = h[k];
    h[k] = idx ++;
}

bool find(int x)
{
    int k =  (x % N + N) % N;
    for(int i = k;i != -1;i = ne[i])
    {
        if(e[i] == x) return true;
    }
    return false;
}

//初始化
memset(h,-1,sizeof h);


//开放寻址法
const int null = 0x3f3f3f3f;
int h[N];
//初始化
memset(h,0x3f,sizeof h);

int insert(int x)//返回在h[]中的索引
{
    int k =  (x % N + N) % N;
    while(h[k] != null && h[k] != x)
    {
        k ++;
        if(k == N) k = 0;
    }
    return k;
}


//查询操作
int x;
if(h[insert(x)] != null)
{
       //查询成功
}
```

#字符串哈希:将字符串转换成P进制,使用前缀和数组进行储存,可利用哈希值快速识别两个子串是否相等

```c++
//核心思想：将字符串看成P进制数，P的经验值是131或13331，取这两个值的冲突概率低
//小技巧：取模的数用2^64，这样直接用unsigned long long存储，溢出的结果就是取模的结果
typedef unsigned long long ULL;
ULL h[N],p[N];

char str[N];
scanf("%s",str + 1);

//初始化
p[0] = 1;
for(int i = 1;i <= n;i ++)
{
    h[i] = h[i - 1] * P + str[i];
    p[i] = p[i-1] * P;
}
//计算子串哈希值
ULL get_hash(int l,int r)
{
    ULL hash_val = h[r] - h[l -1] * p[r - l + 1];
    return hash_val;
}
```
